# Print a BrAPI Connection Object

Print a BrAPI Connection Object

## Usage

``` r
# S3 method for class 'brapi_con'
print(x, ...)
```

## Arguments

- x:

  A `brapi_con` object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
