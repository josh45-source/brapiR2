% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{brapi_connection}
\alias{brapi_connection}
\title{Create a BrAPI Connection Object}
\usage{
brapi_connection(
  url,
  token = NULL,
  version = "v2",
  path = "brapi",
  user_agent = NULL,
  page_size = 1000L,
  timeout = 120
)
}
\arguments{
\item{url}{Character. Base URL of the BrAPI server
(e.g. \code{"https://test-server.brapi.org"}). Trailing slashes are removed.}

\item{token}{Character or NULL. An existing Bearer token for authentication.
If NULL, you can authenticate later with \code{\link[=brapi_login]{brapi_login()}} or
\code{\link[=brapi_login_oauth2]{brapi_login_oauth2()}}.}

\item{version}{Character. BrAPI version path segment. Default \code{"v2"}.}

\item{path}{Character. URL path segment before the version, for servers
that do not serve BrAPI at \verb{/brapi/}. Default \code{"brapi"}. GRIN-Global
instances, for example, use \code{"gringlobal/brapi"}.}

\item{user_agent}{Character or NULL. Overrides the user agent brapiR2
sends with each request. The default identifies the package, its
version, and the httr2 and R versions in use.}

\item{page_size}{Integer. Number of records per page for paginated requests.
Default 1000.}

\item{timeout}{Numeric. Request timeout in seconds. Default 120.}
}
\value{
An S3 object of class \code{"brapi_con"} (a named list).
}
\description{
Creates a connection object that holds server URL, authentication token,
and configuration. This object is passed as the first argument to all
\code{brapiR2} functions. No global state is used.
}
\examples{
# Connect to the public BrAPI test server (no auth needed)
con <- brapi_connection("https://test-server.brapi.org")
con

# Connect with an existing token
con <- brapi_connection("https://my-breedbase.org", token = "my_token_here")

# Connect to a server that serves BrAPI under a different path
con <- brapi_connection("https://npgsweb.ars-grin.gov",
                        path = "gringlobal/brapi")

}
