% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genotyping.R
\name{brapi_get_dosage_matrix}
\alias{brapi_get_dosage_matrix}
\title{Get Dosage Matrix for Genomic Selection}
\usage{
brapi_get_dosage_matrix(con, variantSetDbId, sep = "/", unknown_string = ".")
}
\arguments{
\item{con}{A \code{\link[=brapi_connection]{brapi_connection()}} object.}

\item{variantSetDbId}{Character. The variant set to retrieve.}

\item{sep}{Character. Unphased allele separator. Default \code{"/"} (e.g.
\code{"0/1"}). Phased calls using \code{"|"} are also handled automatically.}

\item{unknown_string}{Character. String representing missing data.
Default \code{"."}.}
}
\value{
A numeric matrix: rows = samples (callSetDbIds), columns = markers
(variantDbIds). Values are integer dosages (0, 1, 2 for diploids; 0–N
for polyploids). Missing calls are \code{NA}.
}
\description{
Fetches genotype data from a BrAPI server and converts it into a numeric
dosage matrix (samples × markers) compatible with genomic selection
packages like \code{rrBLUP}, \code{BGLR}, and \code{sommer}.
}
\details{
Allele dosage is computed by splitting each genotype string on \code{sep} (or
\code{"|"} for phased calls) and counting how many alleles are non-reference
(i.e. not \code{"0"}). Missing calls (\code{unknown_string}, \code{"."}, or \code{""}) become
\code{NA}.
}
\examples{
\dontrun{
con <- brapi_connection("https://test-server.brapi.org")
dosage <- brapi_get_dosage_matrix(con, "variantset_01")
dim(dosage)
# Use with rrBLUP:
# library(rrBLUP)
# result <- mixed.solve(y = pheno$yield, Z = dosage)
}

}
