% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/germplasm.R
\name{brapi_pedigree}
\alias{brapi_pedigree}
\title{List Pedigree Nodes}
\usage{
brapi_pedigree(
  con,
  germplasmDbId = NULL,
  includeParents = NULL,
  includeSiblings = NULL,
  includeProgeny = NULL,
  includeFullTree = NULL,
  pedigreeDepth = NULL,
  progenyDepth = NULL,
  ...
)
}
\arguments{
\item{con}{A \code{\link[=brapi_connection]{brapi_connection()}} object.}

\item{germplasmDbId}{Character or NULL. Filter by germplasm.}

\item{includeParents}{Logical or NULL. Include each node's parents.}

\item{includeSiblings}{Logical or NULL. Include each node's siblings.}

\item{includeProgeny}{Logical or NULL. Include each node's progeny.}

\item{includeFullTree}{Logical or NULL. Recursively include every node
reachable in the pedigree tree.}

\item{pedigreeDepth}{Integer or NULL. Number of levels to include up
the tree (parents, grandparents, ...).}

\item{progenyDepth}{Integer or NULL. Number of levels to include down
the tree (children, grandchildren, ...).}

\item{...}{Additional query parameters.}
}
\value{
A tibble with one row per pedigree node. \code{parents}, \code{siblings},
and \code{progeny}, when requested, are list-columns of small tibbles (one
row per relative) rather than raw nested lists or a flattened table.
}
\description{
Retrieves a filtered subset of a pedigree tree via \verb{/pedigree} - a batch
endpoint for pulling pedigree records across many germplasm in one call.
This is different from \code{\link[=brapi_germplasm_pedigree]{brapi_germplasm_pedigree()}}, which retrieves one
germplasm's immediate pedigree via the germplasm sub-resource
(\verb{/germplasm/\{germplasmDbId\}/pedigree}) and must be called once per
germplasm. Use \code{brapi_pedigree()} (or \code{\link[=brapi_search_pedigree]{brapi_search_pedigree()}}) to pull
pedigree records for many germplasm at once - e.g. everything in a crop,
program, or family - in one or a few requests; use
\code{\link[=brapi_germplasm_pedigree]{brapi_germplasm_pedigree()}} when you already have a single germplasm ID
in hand.
}
\details{
Each row is one pedigree node (one germplasm). The server only includes
a node's relatives if asked: set \code{includeParents}, \code{includeSiblings},
and/or \code{includeProgeny} to \code{TRUE} to populate the \code{parents}, \code{siblings},
and \code{progeny} list-columns, each holding a small tibble of related
germplasm (\code{germplasmDbId}, \code{germplasmName}, and \code{parentType} - \code{NA} for
siblings, which have none) that you can \code{\link[tidyr:unnest]{tidyr::unnest()}} when you need
one row per relationship rather than one row per node. Nodes are never
collapsed or flattened by default: a pedigree is graph-shaped (each node
has its own parents, siblings, and progeny edges), and the three
relation types don't share a common row shape, so there is no lossless
single flat table to fall back to.
}
\examples{
\donttest{
con <- brapi_connection("https://test-server.brapi.org")
brapi_pedigree(con, includeParents = TRUE, includeProgeny = TRUE)
}

}
\seealso{
\code{\link[=brapi_germplasm_pedigree]{brapi_germplasm_pedigree()}} for one germplasm's pedigree via
the germplasm sub-resource; \code{\link[=brapi_search_pedigree]{brapi_search_pedigree()}} for the same
batch retrieval via \code{POST}, with a fuller set of filters.
}
